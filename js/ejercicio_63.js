let edad=parseInt(prompt("Ingrese la edad del usuario: "));
for (let i = 1; i <=edad; i++) {
    if (i==1)
    alert(`HA CUMPLIDO ${i} año` )
    if (i>=2) {
        alert(`HA CUMPLIDO ${i} años` )
    }
}