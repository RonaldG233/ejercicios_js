let nombre=prompt("ingrese su nombre: ");
let genero=prompt("ingrese su genero: ");

let cursos=function (nombre, genero) {
    if (genero==="f"  ) {
        if (nombre=="m..") {
            /[a-mA-M]/
        }
    }
}

console.log(cursos);

